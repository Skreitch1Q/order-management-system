# Система учёта заказов интернет-магазина

Прототип промышленной системы учёта заказов, клиентов и товаров с графическим
интерфейсом (tkinter), базой данных на основе Excel (файл `shop.xlsx`, библиотека
openpyxl) и средствами анализа и визуализации данных (pandas, matplotlib,
seaborn, networkx).

Проект выполнен в рамках итоговой аттестации по Python.

## Возможности

- Регистрация клиентов и их контактных данных (валидация email и телефона
  регулярными выражениями).
- Добавление товаров и создание заказов с несколькими позициями.
- Просмотр, поиск, фильтрация и сортировка заказов и клиентов.
- Импорт и экспорт данных в форматах CSV и JSON.
- Анализ и визуализация данных:
  - Топ 5 клиентов по числу заказов;
  - Динамика количества заказов по датам;
  - Топ товаров по продажам;
  - Выручка по категориям;
  - Граф связей клиентов (по городу / общим товарам).
- Обработка ошибок через `try...except`.
- Unit-тесты для модулей `models.py` и `analysis.py`.

## Структура проекта

```
.
├── main.py          # Точка входа, аргументы командной строки
├── models.py        # Классы данных: Product, Customer, Order (+ валидация)
├── db.py            # База данных в Excel (openpyxl), импорт/экспорт
├── gui.py           # Графический интерфейс на tkinter
├── analysis.py      # Анализ и визуализация данных
├── tests/           # Unit-тесты
│   ├── test_models.py
│   └── test_analysis.py
├── requirements.txt
└── README.md
```

## Установка и запуск

### 1. Создание виртуального окружения и установка зависимостей

```bash
python3 -m venv .venv
source .venv/bin/activate        # Linux / macOS
# .venv\Scripts\activate         # Windows
pip install -r requirements.txt
```

### 2. Заполнение базы демо-данными (опционально)

```bash
python main.py --seed
```

### 3. Запуск приложения

```bash
python main.py
```

### 4. Запуск тестов

```bash
python main.py --test
# или
python -m unittest discover tests
```

## Требования (requirements.txt)

- `pandas`
- `matplotlib`
- `seaborn`
- `networkx`
- `Pillow`
- `openpyxl`

## Хранение данных в Excel

База данных — файл `shop.xlsx` (создаётся автоматически при первом запуске).
Данные распределены по листам книги Excel:

| Лист          | Колонки                                      |
|---------------|----------------------------------------------|
| `products`    | product_id, name, price, category            |
| `customers`   | customer_id, name, email, phone, city        |
| `orders`      | order_id, customer_id, order_date, status    |
| `order_items` | item_id, order_id, product_id, quantity      |

Идентификаторы генерируются автоматически (`_next_id`). Для работы с файлом
используется библиотека `openpyxl`. Данные можно открыть и отредактировать
вручную в любом редакторе таблиц (Excel, LibreOffice, Google Таблицы).

## ООП в проекте

- **Инкапсуляция** — приватные поля (`_name`, `_price` и т.д.) и свойства
  (`@property`) для доступа к ним.
- **Наследование** — базовые классы-проверщики `ValidatedField` и наследники
  `EmailField`, `PhoneField`.
- **Полиморфизм** — единый интерфейс `to_dict()`/`from_dict()` у всех классов
  данных, единообразная обработка в коде.

## Функции и сортировки

- Функции с параметрами по умолчанию (`top_customers(orders, top_n=5)`).
- Рекурсивные и лямбда-выражения (например, `sorted(..., key=lambda o: o.total())`).
- Собственная сортировка заказов по дате и по стоимости во вкладке «Заказы».

## Регулярные выражения

В `models.py` определены шаблоны:

- `EMAIL_RE` — проверка адреса электронной почты;
- `PHONE_RE` — проверка номера телефона в распространённых форматах.

## Скриншоты

*Скриншоты интерфейса добавьте по мере готовности:* например, файлы
`screenshot_main.png`, `screenshot_analysis.png` в корне проекта и изображения
графиков в папке `plots/`.

## Сгенерированные графики

При запуске функций анализа графики сохраняются в папку `plots/`:
- `top_customers.png`
- `orders_dynamics.png`
- `top_products.png`
- `sales_by_category.png`
- `customer_graph.png`

## Лицензия

Учебный проект.
